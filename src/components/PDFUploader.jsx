// Thin alias around MediaUploader for the spec's PDFUploader.jsx slot.
// MediaUploader already accepts both images and PDFs, which is what every
// drawing sub-section needs.
export { default } from './MediaUploader'
